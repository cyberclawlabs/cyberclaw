# Codex Example Skill

这是一个 Codex 格式的示例 Skill，演示如何使用 `manifest.yaml` 定义 Skill 元数据。

## 功能

- 数据转换
- 格式化输出
- 批处理

## 使用

```json
{
  "operation": "transform",
  "data": {...}
}
```

## 配置

参见 `manifest.yaml` 文件。
